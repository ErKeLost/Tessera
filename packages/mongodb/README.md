# @open-tessera/mongodb

MongoDB implementation of the Tessera Agent database connector contract. It
infers a bounded collection catalog from sampled documents and executes only
validated, compiler-produced read-only aggregation pipelines.
