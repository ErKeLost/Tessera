# @open-tessera/database

Framework-neutral, server-only contracts for database connectors, schema catalogs,
connection assessment, and governed query execution. Concrete database drivers live
in separate packages such as `@open-tessera/postgres`.
