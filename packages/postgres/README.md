# @data-elements/postgres

PostgreSQL implementation of the Data Elements database connector contract. It
uses AST validation, bounded results, statement timeouts, and explicit read-only
transactions for every Agent query.
