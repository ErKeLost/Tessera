# @open-tessera/data-agent

Server-only data analysis runtime for Tessera Agent. It discovers database
catalogs, compiles bounded semantic plans, executes governed reads, and routes
mutations through the host approval boundary.
