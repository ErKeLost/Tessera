{
  "name": "open-generative",
  "version": "0.3.9",
  "private": true,
  "description": "Host-governed generative UI protocol, runtime, and official component catalog.",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:docs": "turbo run build --filter=@open-tessera/docs",
    "build:packages": "turbo run build --filter='./packages/*' && bun run check:source",
    "check:boundaries": "bun scripts/check-boundaries.ts",
    "check:naming": "bun scripts/check-naming.ts",
    "check:source": "bun scripts/check-source.ts",
    "lint": "turbo run lint",
    "release:check": "bun run check:naming && bun run check:boundaries && bun run typecheck && bun run build:packages && bun run test && bun scripts/check-release.ts",
    "test": "bun run check:naming && bun run check:boundaries && turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "tsdown": "0.22.14",
    "turbo": "2.10.9",
    "typescript": "7.0.2"
  }
}
