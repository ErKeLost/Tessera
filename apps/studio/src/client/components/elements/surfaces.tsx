"use client";

// The Assistant UI registry resolves this shared item through the `lib` alias.
// Its published Elements keep a relative import, so this preserves that API.
export * from "@/lib/surfaces";
