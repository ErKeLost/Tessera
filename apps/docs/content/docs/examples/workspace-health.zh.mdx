---
title: 工作区健康度
description: 使用正交 Component Contract 组合响应式运行状态快照。
---

这个 Surface 使用 `layout.grid`、`layout.section`、`data.metric`、`data.chart`
与 `content.callout`。每个指标都是独立且经过验证的 Node；任何后续请求都必须经过
已声明 Event Port 与 Host Action Boundary。

<ComponentPreview
  filename="examples/workspace-health.tsx"
  code={`import {
  GenerativeSurface,
  type RendererRegistry,
  type SurfaceControllerPort,
} from "@open-generative/react"

export function WorkspaceHealth({ controller, registry }: {
  controller: SurfaceControllerPort
  registry: RendererRegistry
}) {
  return <GenerativeSurface
    controller={controller}
    registry={registry}
    placement={{ kind: "panel", width: 960, height: 640 }}
  />
}`}
>
  <MetricsDemo />
</ComponentPreview>

Metric、Layout、Chart 与 Content Contract 请查看
[当前 Component Catalog](/docs/components/generative-ui-catalog)。
