---
title: Breakdown Artifact
description: Rank categories by contribution while keeping value, share, change, and evidence together.
---

Use Breakdown Artifact to answer **who contributed to the result**. It is a
ranked analytical answer, not a generic bar chart: every row preserves its
value, share of total, optional period change, and drill-down action.

<ComponentPreview filename="breakdown-artifact-example.tsx" code={`import { BreakdownArtifact } from "@/components/tessera-agent/breakdown-artifact"

export function RevenueByChannel({ artifact }) {
  return <BreakdownArtifact artifact={artifact} locale="en-US" />
}`}>
  <BreakdownDemo />
</ComponentPreview>

## Installation

<InstallCommand name="breakdown-artifact" />

## Selection rule

Use mutually understandable categories measured with the same metric. Do not
use it for sequential stages, overlapping shares, or raw observation ranges.
Selecting a row emits `breakdown-item-select` with its ID and rank.

## API Reference

<ApiReference name="BreakdownArtifact" properties={[
  { name: "artifact", type: "BreakdownArtifactData", required: true, description: "A dimension, metric, optional total, and one to twenty-four ranked contribution items." },
  { name: "locale", type: "string", defaultValue: '"en-US"', description: "Locale used for values, shares, and category labels." },
]} />
