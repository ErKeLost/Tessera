---
title: Cohort Artifact
description: Compare retention or conversion across cohort start dates and consistent elapsed periods.
---

> **Historical design input:** This page preserves the component design, examples, and visual intent of an earlier fixed Artifact recipe. It is not a current Component Contract or public installation API. The current data-agent Generative UI composes this experience from the [12 orthogonal Contracts](/docs/components/generative-ui-catalog).


Cohort Artifact renders a compact heatmap with cohort size and incomplete
recent periods kept explicit. Rows align by cohort age, so calendar totals
cannot be mistaken for retention.

<ComponentPreview filename="cohort-artifact-example.tsx" code={`import { CohortArtifact } from "@/components/tessera-agent/cohort-artifact"

export function WorkspaceRetention({ artifact }) {
  return <CohortArtifact artifact={artifact} locale="en-US" />
}`}>
  <CohortDemo />
</ComponentPreview>

## Installation

<InstallCommand name="cohort-artifact" />

## Selection rule

Use identical cohort membership and period definitions for every row. Use
`null` for periods that have not matured, never zero. Selecting a populated
cell emits `cohort-cell-select` with cohort, period, and value.

## API Reference

<ApiReference name="CohortArtifact" properties={[
  { name: "artifact", type: "CohortArtifactData", required: true, description: "Period labels and cohort rows with size and one nullable percentage per period." },
  { name: "locale", type: "string", defaultValue: '"en-US"', description: "Locale used for cohort sizes and percentages." },
]} />
