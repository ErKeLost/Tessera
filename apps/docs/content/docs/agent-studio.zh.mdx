---
title: Tessera Data Agent Studio
description: 将自然语言数据问题转化为经过验证、可审查分析的受治理工作台。
---

Tessera Data Agent Studio 是一个 Local-first 工作台，用于向已连接的数据提问并发布可审查的答案。
它会把自然语言请求转化为语义计划、受限的只读查询、经过验证的证据，以及 UI 可以渲染的结构化
Artifact。

它不是在数据库外面包一层 Chat 的 Agent。模型只提出语义意图；Catalog 访问、授权边界、查询编译、
执行、验证、证据限制和发布都由 Tessera 掌控。

![Tessera Data Agent Studio 架构](/diagrams/tessera-data-agent-studio.svg)

## 使用 Studio

Studio 是一个本地 Web 工作台。你在包含 Tessera 配置的目录中启动它，
然后用浏览器打开终端输出的地址。数据库连接和模型凭据留在服务端进程中；
浏览器只会收到 Catalog 摘要、进度、安全叙述和经过验证的结果。

### 1. 准备环境

- Node.js 20.18.1 或更高版本
- Bun 1.3.14 或更高版本
- PostgreSQL 或 MySQL 数据库，建议使用只读数据库用户
- 配置中所选模型供应商的 API Key

### 2. 创建只在服务端使用的配置

在项目目录创建 `tessera.config.ts`，并把密钥放入 `.env`（或启动 Studio
的服务端环境）：

```bash
DATABASE_URL=postgresql://readonly:password@127.0.0.1:5432/warehouse
OPENROUTER_API_KEY=your-server-only-key
```

```ts
// tessera.config.ts
import { defineTesseraConfig } from "@data-elements/studio";

const databaseUrl = process.env.DATABASE_URL;
if (!databaseUrl) throw new Error("DATABASE_URL is required by Tessera Studio.");

export default defineTesseraConfig({
  database: {
    url: databaseUrl,
    // 可选：只允许指定 Schema 进入 Catalog 发现范围。
    // schemas: ["analytics"],
    maxRows: 500,
    statementTimeoutMs: 15_000,
  },
  llm: {
    model: "openrouter/qwen/qwen3.8-27b",
    // 省略 apiKey 时，会从服务端环境读取 OPENROUTER_API_KEY。
  },
});
```

默认模型是 OpenRouter。你也可以填写其他 Mastra 支持的 `provider/model`
ID，并提供对应的供应商环境变量，或配置 OpenAI 兼容的 `baseUrl`。不要把
任何密钥放进客户端代码或会暴露给浏览器的环境变量。

### 3. 启动本地 Web 服务

在同一目录执行发布后的 CLI：

```bash
npx data-elements@latest studio
```

CLI 会从当前目录向上查找 `tessera.config.ts`，并在
`http://127.0.0.1:4317` 启动 Studio。临时测试时，也可以直接传入一个
数据库 URL：

```bash
npx data-elements@latest studio postgresql://readonly:password@127.0.0.1:5432/warehouse
npx data-elements@latest studio mysql://readonly:password@127.0.0.1:3306/warehouse
```

临时 URL 适合测试，但可能出现在 Shell 历史或进程列表中。长期使用时，
建议采用配置文件和环境变量。需要显式指定配置文件或端口时：

```bash
npx data-elements@latest studio \
  --config ./config/tessera.config.ts \
  --host 127.0.0.1 \
  --port 4310
```

### 4. 完成一次分析

1. **连接**：先确认工作台顶部的数据库状态。如果连接缺失或已过期，打开
   **Settings → Database**，修改 URL 或数据库类型，测试连接后保存。
2. **查看 Catalog**：进入 **Data explorer** 浏览 Schema、表、字段、键、定义
   和代表性数据。数据库完成 Schema 迁移后，刷新 Catalog。
3. **提出问题**：新建一个 Thread，用自然语言描述业务问题。尽量补充时间范围、
   维度、指标和过滤条件。
4. **检查运行过程**：答案旁会显示 Catalog 检查、计划、执行、验证和叙述进度。
   展开运行记录可以查看可公开的证据；原始 SQL 和凭据仍留在服务端。
5. **完善结果**：在同一个 Thread 中继续追问、澄清歧义，或在调整范围后重试。
   每次分析执行前都会针对当前 Catalog 重新验证。
6. **管理历史**：使用 **History** 重新打开、重命名或删除本地分析 Thread。
   会话历史保存于 Studio 自己的本地 SQLite，与被分析的数据库分离。

### 5. 调整工作台

打开 **Settings** 管理当前工作台：

| 区域 | 用途 |
| --- | --- |
| Database | 连接 URL、数据库类型、Schema 范围和读写模式 |
| Model | 供应商、模型、API Key、Base URL 和推理强度 |
| Limits | 最大行数、语句超时和 Agent 步数限制 |
| Permissions | 读取、写入、破坏性操作和未知 SQL 的策略 |

设置由服务端 Runtime 应用。敏感设置可以由宿主的身份认证和管理员策略
限制。除非有明确且经过审查的需求，否则请保持数据库用户为只读。

## 一次运行中 Agent 做了什么

完整的用户路径可以概括为：

```text
问题 -> Catalog -> 语义计划 -> 受限只读查询
    -> 验证 -> 证据 -> Markdown 答案
```

模型通过语义 ID 提出意图。Data Agent 加载实时 Catalog，检查 Capability
和授权范围，在服务端编译查询，应用行数和时间限制，验证结果并发布安全响应。
当前本地 Studio 以 Markdown、紧凑的执行进度和证据呈现响应；连接字符串、供应商
Key、原始 SQL 和数据库驱动都不会发送到浏览器。

## Studio 做什么

Studio 让一次运行始终只有一个回答责任主体，同时把足够的进度暴露给用户，使用户能够检查、
中断、重试或澄清任务。

| 步骤 | 用户看到的结果 | 服务端职责 |
| --- | --- | --- |
| 连接 | 工作台中出现已配置的数据源 | 宿主提供 Connector，并把凭据留在服务端 |
| 发现 | 出现相关的业务实体、字段、指标和关系 | Data Agent 加载并缓存实时 Catalog，然后返回受限的语义切片 |
| 澄清 | 把歧义变成明确问题或已确认的选择 | Agent 可以描述已检查的实体，并执行有限的发现 Probe |
| 计划 | Runtime 可以审查只读分析意图 | 模型提交带语义 ID 的 `AnalysisDraft`，绝不提交 SQL 或物理名称 |
| 执行 | 结果、Trace 和证据流入对话 | Data Agent 重新验证范围、绑定 ID、编译 SQL、按限制执行并验证结果 |
| 发布 | 经过验证的 Markdown 答案和证据随运行结果呈现 | Tessera 限制证据，并在发布前脱敏私有 Runtime 细节 |

默认实现位于 `apps/studio`。受治理的执行边界是 `@data-elements/data-agent`；宿主可以注入 Connector、
身份提供器、Session Store、模型或兼容的 `StudioAgent`，而不必改变公开协议。

## Agent Tool 契约

面向模型的 Tool 集合刻意保持很小：

```text
inspect_current_context  -> 服务端绑定的当前数据上下文
inspect_catalog          -> 针对一个问题的语义 Catalog 切片
describe_data            -> 已检查实体的受限详细信息
probe_data               -> 固定且受限的发现 Probe
run_analysis             -> 根据语义 Draft 执行只读分析
```

Tool 只能使用当前 Catalog 返回的语义 ID。它们不能接收 SQL、连接字符串、凭据、物理关系名、Compiler
输出 ID 或任意查询限制。`probe_data` 用于解决实质性歧义，不是第二个浏览记录的循环。最终答案必须
建立在经过验证的 `run_analysis` 结果上。

## 公开生命周期

UI 接收的是面向产品的生命周期，而不是私有 Tool Payload。公开阶段按固定顺序呈现，使用户能够知道
运行停在哪里以及为什么停止：

| 阶段 | 含义 |
| --- | --- |
| `catalog` | 加载实时物理与语义 Catalog |
| `retrieval` | 选择与请求相关的语义上下文 |
| `planning` | 绑定安全的分析解释和 Capability Scope |
| `probing` | 解决受限的值域、字段 Profile 或 Join 歧义 |
| `compiling` | 把经过验证的语义 Draft 编译为服务端查询 |
| `executing` | 通过 Connector 执行受限的只读查询 |
| `verifying` | 检查结果形状、限制、截断情况和证据质量 |
| `publishing` | 发布经过验证的结构化 Artifact 与证据引用 |
| `narrating` | 流式输出安全的最终说明 |

只有摘要会穿过 Browser 边界：Tool 名称与状态、阶段状态、受限耗时、安全叙述和证据引用。原始 Tool
参数、SQL、Catalog ID、Connector 元数据和不受限查询输出都留在服务端。

## 设计架构

### Experience 平面

React Studio UI 负责对话体验：Threads、Chat、表格预览、执行 Trace、Artifact、来源引用和人工控制。
它消费 Tessera UI-message 协议，绝不直接调用数据库 Connector。

### Studio Server 与 Agent 平面

Hono Server 是 `/api/chat`、`/api/catalog`、`/api/runs`、Settings、Threads 和表格预览的传输边界。
它调用模型循环，脱敏不可信的 Stream Chunk，把完整消息持久化到服务端 Local SQLite，并且只发布公开协议。

### 受治理 Data Agent 平面

`@data-elements/data-agent` 负责 Catalog 加载与缓存、语义绑定、短期签名的 Planning Capability、类型化
发现 Probe、SQL 编译、Connector 执行和结果验证。在绑定和执行分析前，它会再次检查实时 Catalog Fingerprint，
因此过期计划不能悄悄运行在已变化的 Schema 上。

### 宿主与数据平面

嵌入宿主负责 Authentication、Tenant Membership、Role 和 Policy。Connector 提供 Catalog Introspection
与参数化执行。数据库写入是独立的类型化 Action Surface，需要 Policy 检查和 Approval Checkpoint；它不是
只读 Agent 循环的延伸。

## Trust Boundary

| 边界 | 允许 | 永远不允许 |
| --- | --- | --- |
| Model | 语义 Catalog 切片、Opaque ID、受限证据、明确的下一步 | SQL、凭据、物理表名、任意 Tool Payload |
| Browser | 脱敏生命周期数据、安全叙述、经过验证的 Artifact | 原始 Tool 参数、私有 Trace、直接访问 Connector |
| Studio Server | Authentication Hook、编排、Stream 过滤、Session 持久化 | 自行解析或生成宿主凭据；绕过宿主授权 |
| Data Agent | 实时 Catalog、签名 Scope、类型化 Draft、受限查询结果 | 调用方提供的 SQL，或超出当前 Capability 的分析 |
| Write Surface | 类型化 Mutation、用途、幂等键、Approval 决策 | 只读分析中的隐式写入或自由 SQL |

这些边界让模型成为意图规划器，而不是数据库 Principal。宿主仍然决定谁可以访问哪个 Tenant，
Data Agent 则决定已获准的语义读取如何变成查询。

## 实现对应关系

| 关注点 | 实现 |
| --- | --- |
| 产品 Shell 与路由 | `apps/studio/src/server.ts` 与 `apps/studio/src/client` |
| 默认 Agent 编排 | `apps/studio/src/agent.ts` |
| 公开 Stream 与阶段类型 | `apps/studio/src/protocol.ts` |
| Catalog、Capability、Compiler、执行 | `packages/data-agent/src/index.ts` 及其 Compiler 模块 |
| Session Memory | 服务端/Local SQLite，与被分析数据库隔离 |
| 可选写入 | 带 Policy 与 Approval Gate 的类型化 Database Action Service |

## 继续阅读架构参考

实现或审查生产集成时，可以继续阅读这些页面：

<Cards>
  <Card title="Agent 架构" description="四个运行平面、契约和生产边界的参考拓扑。" href="/docs/agent-architecture" />
  <Card title="运行循环" description="请求如何从澄清经过验证并完成发布。" href="/docs/agent-architecture/run-loop" />
  <Card title="Tool 与 SQL 安全" description="为什么语义 Tool 与服务端编译必须分离。" href="/docs/agent-architecture/tool-and-sql-safety" />
  <Card title="证据与溯源" description="受限结果如何成为论断级证据。" href="/docs/agent-architecture/evidence-and-provenance" />
  <Card title="评估与生产准入" description="Agent 接入生产数据前必须通过的门槛。" href="/docs/agent-architecture/evals-and-production-gates" />
</Cards>
